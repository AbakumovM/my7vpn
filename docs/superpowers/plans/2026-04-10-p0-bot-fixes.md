# P0 Bot Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Устранить три критичных бага: неатомарная транзакция при создании устройства, двойное нажатие кнопки "Оплачено", и небезопасный парсинг callback_data через split().

**Architecture:**
- Атомарность достигается переносом `deduct_balance` внутрь `DeviceInteractor` — оба действия теперь в одном `uow.commit()`.
- Идемпотентность реализуется немедленным удалением клавиатуры до начала обработки платежа.
- Безопасный парсинг — через добавление типизированных `CallbackData`-классов в `cbdata.py` и замену строковых обработчиков.

**Tech Stack:** Python 3.12, Aiogram 3, Dishka, SQLAlchemy async, pytest-asyncio

---

## Затрагиваемые файлы

| Действие | Файл | Что меняется |
|----------|------|-------------|
| Modify | `src/apps/device/domain/commands.py` | Добавить `balance_to_deduct: int = 0` в `CreateDevice` и `RenewSubscription` |
| Modify | `src/apps/device/application/interactor.py` | Перенести deduct_balance внутрь create_device / renew_subscription |
| Modify | `src/common/bot/cbdata.py` | Добавить `DeviceConfCallback`, `DeviceDeleteCallback`, `DeviceErrorCallback`, `SettingsCallback` |
| Modify | `src/common/bot/keyboards/keyboards.py` | Использовать новые CallbackData вместо f-строк |
| Modify | `src/apps/device/controllers/bot/router.py` | Идемпотентность + убрать отдельный deduct_balance + новые CallbackData-фильтры |
| Modify | `src/common/bot/router.py` | Заменить split(":") на DeviceErrorCallback и SettingsCallback |
| Modify | `tests/unit/device/test_device_interactor.py` | Добавить тесты на атомарность |

---

## Task 1: Атомарная транзакция — команды

**Файлы:**
- Modify: `src/apps/device/domain/commands.py`

- [ ] **Step 1: Добавить поля в команды**

В `src/apps/device/domain/commands.py` изменить `CreateDevice` и `RenewSubscription`:

```python
from dataclasses import dataclass, field


@dataclass(frozen=True)
class CreateDevice:
    telegram_id: int
    device_type: str
    period_months: int
    amount: int
    balance_to_deduct: int = 0


@dataclass(frozen=True)
class CreateDeviceFree:
    telegram_id: int
    device_type: str
    period_days: int


@dataclass(frozen=True)
class DeleteDevice:
    device_id: int


@dataclass(frozen=True)
class RenewSubscription:
    device_name: str
    period_months: int
    amount: int
    balance_to_deduct: int = 0


@dataclass(frozen=True)
class GetExpiringSubscriptions:
    pass
```

- [ ] **Step 2: Убедиться что файл сохранён корректно**

```bash
python -c "from src.apps.device.domain.commands import CreateDevice, RenewSubscription; print('OK')"
```
Ожидание: `OK`

---

## Task 2: Атомарная транзакция — Interactor

**Файлы:**
- Modify: `src/apps/device/application/interactor.py`

- [ ] **Step 1: Написать падающий тест**

Добавить в `tests/unit/device/test_device_interactor.py`:

```python
@pytest.mark.asyncio
async def test_create_device_deducts_balance_atomically(
    interactor: DeviceInteractor,
    mock_gateway: AsyncMock,
    mock_user_gateway: AsyncMock,
    mock_uow: AsyncMock,
) -> None:
    """Если balance_to_deduct > 0 — списание и создание устройства в одном commit."""
    from src.apps.user.domain.models import User

    user = User(telegram_id=123, balance=100)
    mock_user_gateway.get_by_telegram_id.return_value = user
    mock_gateway.get_next_seq.return_value = 1

    cmd = CreateDevice(
        telegram_id=123,
        device_type="Android",
        period_months=1,
        amount=150,
        balance_to_deduct=50,
    )
    result = await interactor.create_device(cmd)

    assert user.balance == 50
    mock_uow.commit.assert_called_once()  # только один коммит!
    assert result.device_name.startswith("Android")


@pytest.mark.asyncio
async def test_renew_subscription_deducts_balance_atomically(
    interactor: DeviceInteractor,
    mock_gateway: AsyncMock,
    mock_uow: AsyncMock,
) -> None:
    from datetime import UTC, datetime
    from src.apps.device.domain.models import Device, Subscription

    sub = Subscription(device_id=1, plan=1, start_date=datetime.now(UTC), end_date=datetime.now(UTC))
    device = Device(user_id=123, device_name="Android 1", created_at=datetime.now(UTC), subscription=sub)
    mock_gateway.get_by_name.return_value = device

    # Нужен пользователь с балансом для списания
    from src.apps.user.domain.models import User
    user = User(telegram_id=123, balance=100)
    mock_user_gateway = interactor._user_gateway
    mock_user_gateway.get_by_telegram_id.return_value = user

    cmd = RenewSubscription(device_name="Android 1", period_months=1, amount=150, balance_to_deduct=30)
    result = await interactor.renew_subscription(cmd)

    assert user.balance == 70
    mock_uow.commit.assert_called_once()
```

- [ ] **Step 2: Запустить тест — убедиться что падает**

```bash
uv run pytest tests/unit/device/test_device_interactor.py::test_create_device_deducts_balance_atomically -v
```
Ожидание: `FAILED` — `TypeError: CreateDevice.__init__() got an unexpected keyword argument 'balance_to_deduct'` (или что balance не изменился)

- [ ] **Step 3: Перенести логику deduct_balance в Interactor**

В `src/apps/device/application/interactor.py` изменить `create_device` и `renew_subscription`:

```python
from src.apps.user.domain.exceptions import InsufficientBalance


async def create_device(self, cmd: CreateDevice) -> DeviceCreatedInfo:
    user = await self._user_gateway.get_by_telegram_id(cmd.telegram_id)
    if user is None:
        raise UserDeviceNotFound(cmd.telegram_id)

    if cmd.balance_to_deduct > 0:
        if user.balance < cmd.balance_to_deduct:
            raise InsufficientBalance(cmd.telegram_id, user.balance, cmd.balance_to_deduct)
        user.balance -= cmd.balance_to_deduct
        await self._user_gateway.save(user)

    device_name = await self._generate_device_name(cmd.device_type)
    now = datetime.now(UTC)
    end_date = now + relativedelta(months=cmd.period_months)

    subscription = Subscription(
        device_id=0,
        plan=cmd.period_months,
        start_date=now,
        end_date=end_date,
    )
    payment = Payment(
        subscription_id=0,
        amount=cmd.amount,
        payment_date=now,
    )
    subscription_with_payment = subscription
    device = Device(
        user_id=user.telegram_id,
        device_name=device_name,
        created_at=now,
        subscription=subscription_with_payment,
    )
    device.subscription.payments = [payment]  # type: ignore[attr-defined]

    await self._gateway.save(device)
    await self._uow.commit()
    return DeviceCreatedInfo(device_name=device_name, user_telegram_id=cmd.telegram_id)


async def renew_subscription(self, cmd: RenewSubscription) -> SubscriptionInfo:
    device = await self._gateway.get_by_name(cmd.device_name)
    if device is None:
        raise DeviceNotFound(device_name=cmd.device_name)
    if device.subscription is None:
        raise SubscriptionNotFound(device.id or 0)

    if cmd.balance_to_deduct > 0:
        user = await self._user_gateway.get_by_telegram_id(device.user_id)
        if user is not None:
            if user.balance < cmd.balance_to_deduct:
                raise InsufficientBalance(device.user_id, user.balance, cmd.balance_to_deduct)
            user.balance -= cmd.balance_to_deduct
            await self._user_gateway.save(user)

    now = datetime.now(UTC)
    sub = device.subscription
    base = sub.end_date if sub.end_date > now else now
    sub.end_date = base + relativedelta(months=cmd.period_months)
    sub.plan = cmd.period_months
    sub.start_date = now

    await self._gateway.save(device)
    await self._uow.commit()
    return SubscriptionInfo(
        device_name=device.device_name,
        end_date=sub.end_date,
        plan=sub.plan,
    )
```

- [ ] **Step 4: Запустить тесты — должны пройти**

```bash
uv run pytest tests/unit/device/ -v
```
Ожидание: все тесты `PASSED`

- [ ] **Step 5: Убрать отдельные вызовы deduct_balance в контроллере**

В `src/apps/device/controllers/bot/router.py` в хендлере `handle_vpn_flow`:

Заменить блок NEW_SUB (строки ~359-394):
```python
# Шаг 6a: новая подписка — оплата успешна
if action == CallbackAction.NEW_SUB and payment_status == PaymentStatus.SUCCESS:
    result = await interactor.create_device(
        CreateDevice(
            telegram_id=call.from_user.id,
            device_type=device,
            period_months=duration,
            amount=payment,
            balance_to_deduct=balance,  # ← передаём сюда
        )
    )
    log.info(
        "device_created",
        device_name=result.device_name,
        device_type=device,
        period_months=duration,
        amount=payment,
    )
    await call.message.delete()
    await call.message.answer(
        text=bot_repl.get_message_success_payment(), reply_markup=return_start()
    )
    # ← убрать блок: if balance > 0: await user_interactor.deduct_balance(...)
    await bot.send_message(
        chat_id=ADMIN_ID,
        text=bot_repl.send_message_admin_new_device(
            username=call.from_user.username,
            user_id=call.from_user.id,
            device=result.device_name,
            duration=duration,
            payment=payment,
        ),
    )
    await call.answer()
    return
```

Заменить блок RENEW (строки ~397-430):
```python
# Шаг 6b: продление подписки
if action == VpnAction.RENEW and payment_status == PaymentStatus.SUCCESS:
    result_renew = await interactor.renew_subscription(
        RenewSubscription(
            device_name=callback_data.device_name or device,
            period_months=duration,
            amount=payment,
            balance_to_deduct=balance,  # ← передаём сюда
        )
    )
    log.info(
        "subscription_renewed",
        device_name=result_renew.device_name,
        period_months=duration,
        amount=payment,
    )
    await call.message.delete()
    await call.message.answer(
        text=bot_repl.get_message_success_payment_update(), reply_markup=return_start()
    )
    # ← убрать блок: if balance > 0: await user_interactor.deduct_balance(...)
    await bot.send_message(
        chat_id=ADMIN_ID,
        text=bot_repl.send_messages_for_admin_update(
            username=call.from_user.username,
            user_id=call.from_user.id,
            device=device,
            duration=duration,
            payment=payment,
        ),
    )
    await call.answer()
    return
```

- [ ] **Step 6: Проверить импорты — `user_interactor` больше не нужен в handle_vpn_flow**

Если `user_interactor` используется только в handle_vpn_flow для deduct_balance — удалить его из параметров функции. Проверить: используется ли он ещё (для `set_email` он нужен в `handle_email_input`). Если да — оставить, просто убрать вызовы deduct_balance.

- [ ] **Step 7: Commit**

```bash
git add src/apps/device/domain/commands.py src/apps/device/application/interactor.py src/apps/device/controllers/bot/router.py tests/unit/device/test_device_interactor.py
git commit -m "fix: make device creation and balance deduction atomic in single UoW commit"
```

---

## Task 3: Идемпотентность кнопки "Оплачено"

**Файлы:**
- Modify: `src/apps/device/controllers/bot/router.py`

- [ ] **Step 1: Добавить защиту от двойного нажатия**

В `handle_vpn_flow`, в начало блоков `payment_status == PaymentStatus.SUCCESS` (шаги 6a, 6b, 6c) добавить немедленное удаление клавиатуры. Это делается **до** любых DB-операций.

В шаге 6a:
```python
if action == CallbackAction.NEW_SUB and payment_status == PaymentStatus.SUCCESS:
    # Немедленно убираем кнопки — защита от двойного нажатия
    try:
        await call.message.edit_reply_markup(reply_markup=None)
    except Exception:
        pass  # Сообщение уже изменено (второй клик) — выходим молча
    await call.answer()

    result = await interactor.create_device(...)
    ...
    return
```

В шаге 6b:
```python
if action == VpnAction.RENEW and payment_status == PaymentStatus.SUCCESS:
    try:
        await call.message.edit_reply_markup(reply_markup=None)
    except Exception:
        pass
    await call.answer()

    result_renew = await interactor.renew_subscription(...)
    ...
    return
```

В шаге 6c (referral):
```python
if action == VpnAction.REFERRAL:
    try:
        await call.message.edit_reply_markup(reply_markup=None)
    except Exception:
        pass
    await call.answer()

    result_free = await interactor.create_device_free(...)
    ...
    return
```

- [ ] **Step 2: Проверить что бот запускается**

```bash
uv run python -c "from src.apps.device.controllers.bot.router import router; print('OK')"
```
Ожидание: `OK`

- [ ] **Step 3: Commit**

```bash
git add src/apps/device/controllers/bot/router.py
git commit -m "fix: prevent double payment processing by removing keyboard before DB operations"
```

---

## Task 4: Безопасный парсинг — новые CallbackData классы

**Файлы:**
- Modify: `src/common/bot/cbdata.py`

- [ ] **Step 1: Добавить типизированные классы**

В `src/common/bot/cbdata.py`:

```python
from aiogram.filters.callback_data import CallbackData

from src.common.bot.keyboards.user_actions import ChoiceType, PaymentStatus, VpnAction


class VpnCallback(CallbackData, prefix="vpn"):
    action: VpnAction | None = None
    device: str | None = None
    device_name: str | None = None
    duration: int | None = 0
    referral_id: int | None = None
    payment: int | None = None
    balance: int | None = None
    choice: ChoiceType | None = None
    payment_status: PaymentStatus | None = None


class DeviceConfCallback(CallbackData, prefix="conf"):
    device_id: int


class DeviceDeleteCallback(CallbackData, prefix="appr_del_device"):
    device_id: int


class DeviceErrorCallback(CallbackData, prefix="report__device_error"):
    device_id: int


class SettingsCallback(CallbackData, prefix="settings"):
    platform: str
```

> Примечание: prefix у `DeviceErrorCallback` — `report__device_error` (двойное подчёркивание), т.к. Aiogram использует `:` как разделитель в pack() и `report:device_error` конфликтует.

- [ ] **Step 2: Проверить импорт**

```bash
python -c "from src.common.bot.cbdata import DeviceConfCallback, DeviceDeleteCallback, DeviceErrorCallback, SettingsCallback; print('OK')"
```
Ожидание: `OK`

---

## Task 5: Безопасный парсинг — обновить клавиатуры

**Файлы:**
- Modify: `src/common/bot/keyboards/keyboards.py`

- [ ] **Step 1: Заменить f-строки в keyboard-функциях на новые CallbackData**

В `src/common/bot/keyboards/keyboards.py` добавить импорт:
```python
from src.common.bot.cbdata import (
    VpnCallback,
    DeviceConfCallback,
    DeviceDeleteCallback,
    SettingsCallback,
)
```

Изменить `get_keyboard_devices` — кнопки с `conf:{device.id}` → `DeviceConfCallback`:
```python
def get_keyboard_devices(devices: list, conf) -> InlineKeyboardMarkup:
    keyboard = InlineKeyboardMarkup(inline_keyboard=[])
    for device in devices:
        keyboard.inline_keyboard.append(
            [
                InlineKeyboardButton(
                    text=device.device_name,
                    callback_data=DeviceConfCallback(device_id=device.id).pack(),
                )
            ]
        )
    menu = get_basic_menu()
    for i in menu:
        keyboard.inline_keyboard.append(i)
    return keyboard
```

Изменить `get_keyboard_devices_for_del` — кнопки `appr_del_device:{device.id}` → `DeviceDeleteCallback`:
```python
def get_keyboard_devices_for_del(devices: list) -> InlineKeyboardMarkup:
    keyboard = InlineKeyboardMarkup(inline_keyboard=[])
    for device in devices:
        keyboard.inline_keyboard.append(
            [
                InlineKeyboardButton(
                    text=device.device_name,
                    callback_data=DeviceDeleteCallback(device_id=device.id).pack(),
                )
            ]
        )
    return keyboard
```

Изменить `get_keyboard_devices` при вызове с `CallbackAction.DEVICE_ERROR` — в `common/bot/router.py` используется `get_keyboard_devices(devices, CallbackAction.DEVICE_ERROR)`. Этот случай нужно отделить. Добавить отдельную клавиатуру для ошибок:

```python
def get_keyboard_devices_for_error(devices: list) -> InlineKeyboardMarkup:
    from src.common.bot.cbdata import DeviceErrorCallback
    keyboard = InlineKeyboardMarkup(inline_keyboard=[])
    for device in devices:
        keyboard.inline_keyboard.append(
            [
                InlineKeyboardButton(
                    text=device.device_name,
                    callback_data=DeviceErrorCallback(device_id=device.id).pack(),
                )
            ]
        )
    return keyboard
```

Изменить `get_keyboard_help` — кнопки `settings:android_phone` и т.д. → `SettingsCallback`:
```python
# settings-кнопки в get_keyboard_help():
InlineKeyboardButton(
    text=LEXICON_INLINE_RU[CallbackAction.SETTINGS_ANDROID_PHONE],
    callback_data=SettingsCallback(platform="android_phone").pack(),
),
InlineKeyboardButton(
    text=LEXICON_INLINE_RU[CallbackAction.SETTINGS_IOS],
    callback_data=SettingsCallback(platform="ios").pack(),
),
InlineKeyboardButton(
    text=LEXICON_INLINE_RU[CallbackAction.SETTINGS_DESKTOP],
    callback_data=SettingsCallback(platform="desktop").pack(),
),
```

- [ ] **Step 2: Обновить экспорт в keyboards.py**

Добавить `get_keyboard_devices_for_error` в список доступных функций (если есть `__all__` — добавить туда, иначе просто убедиться что функция определена в файле).

- [ ] **Step 3: Проверить импорт**

```bash
python -c "from src.common.bot.keyboards.keyboards import get_keyboard_devices_for_error; print('OK')"
```
Ожидание: `OK`

---

## Task 6: Безопасный парсинг — обновить хендлеры

**Файлы:**
- Modify: `src/apps/device/controllers/bot/router.py`
- Modify: `src/common/bot/router.py`

- [ ] **Step 1: Обновить хендлер handle_device_detail в device/controllers/bot/router.py**

Заменить:
```python
@router.callback_query(F.data.startswith("conf"))
async def handle_device_detail(
    call: types.CallbackQuery,
    device_view: FromDishka[DeviceView],
) -> None:
    device_id = int(call.data.split(":")[1])
```

На:
```python
from src.common.bot.cbdata import DeviceConfCallback, DeviceDeleteCallback

@router.callback_query(DeviceConfCallback.filter())
async def handle_device_detail(
    call: types.CallbackQuery,
    callback_data: DeviceConfCallback,
    device_view: FromDishka[DeviceView],
) -> None:
    device_id = callback_data.device_id
```

- [ ] **Step 2: Обновить хендлер handle_delete_confirm в device/controllers/bot/router.py**

Заменить:
```python
@router.callback_query(F.data.startswith("appr_del_device"))
async def handle_delete_confirm(
    call: types.CallbackQuery,
    bot: Bot,
    interactor: FromDishka[DeviceInteractor],
) -> None:
    device_id = int(call.data.split(":")[1])
```

На:
```python
@router.callback_query(DeviceDeleteCallback.filter())
async def handle_delete_confirm(
    call: types.CallbackQuery,
    callback_data: DeviceDeleteCallback,
    bot: Bot,
    interactor: FromDishka[DeviceInteractor],
) -> None:
    device_id = callback_data.device_id
```

- [ ] **Step 3: Обновить хендлер handle_device_error_report в common/bot/router.py**

Добавить импорт и заменить хендлер:
```python
from src.common.bot.cbdata import DeviceErrorCallback

@router.callback_query(DeviceErrorCallback.filter())
async def handle_device_error_report(
    call: types.CallbackQuery,
    callback_data: DeviceErrorCallback,
    bot: Bot,
) -> None:
    device_id = callback_data.device_id
    log.info("vpn_error_reported", device_id=device_id)
    await bot.send_message(
        chat_id=ADMIN_ID,
        text=(
            f"Пользователь сообщил о проблеме с подключением!\n"
            f"👤 Имя: {call.from_user.username}\n"
            f"🆔 ID: {call.from_user.id}|{device_id}"
        ),
    )
    keyboard = create_inline_kb(
        1,
        CallbackAction.VPN_ERROR,
        CallbackAction.LIST_DEVICES,
        CallbackAction.SUPPORT_HELP,
    )
    await call.message.answer(bot_repl.get_message_admin_error(), reply_markup=keyboard)
```

Также обновить вызов в `handle_vpn_error`:
```python
from src.common.bot.keyboards.keyboards import get_keyboard_devices_for_error

@router.callback_query(F.data == CallbackAction.VPN_ERROR)
async def handle_vpn_error(
    call: types.CallbackQuery,
    device_view: FromDishka[DeviceView],
) -> None:
    try:
        devices = await device_view.list_for_user(call.from_user.id)
        if devices:
            await call.message.answer(
                "С каким устройством у вас возникли проблемы? Пожалуйста, выберите из списка ниже:",
                reply_markup=get_keyboard_devices_for_error(devices),  # ← новая функция
            )
        else:
            await call.message.answer("У вас нет активных устройств")
    except Exception:
        log.exception("handle_vpn_error_error")
        await call.message.answer(
            "Что то пошло не так. Попробуй позже или напиши в поддержку @my7vpnadmin."
        )
```

- [ ] **Step 4: Обновить хендлер handle_settings в common/bot/router.py**

Заменить:
```python
@router.callback_query(F.data.startswith("settings:"))
async def handle_settings(call: types.CallbackQuery) -> None:
    settings_map = {
        "android_phone": bot_repl.get_android_settings(),
        "desktop": bot_repl.get_computer_settings(),
        "ios": bot_repl.get_settings_iphone(),
    }
    settings_type = call.data.split(":")[1]
    text = settings_map.get(settings_type, "Настройки не найдены")
```

На:
```python
from src.common.bot.cbdata import SettingsCallback

@router.callback_query(SettingsCallback.filter())
async def handle_settings(call: types.CallbackQuery, callback_data: SettingsCallback) -> None:
    settings_map = {
        "android_phone": bot_repl.get_android_settings(),
        "desktop": bot_repl.get_computer_settings(),
        "ios": bot_repl.get_settings_iphone(),
    }
    text = settings_map.get(callback_data.platform, "Настройки не найдены")
```

- [ ] **Step 5: Проверить что роутеры импортируются без ошибок**

```bash
uv run python -c "
from src.apps.device.controllers.bot.router import router as dr
from src.common.bot.router import router as cr
print('OK')
"
```
Ожидание: `OK`

- [ ] **Step 6: Запустить все unit-тесты**

```bash
uv run pytest tests/unit/ -v
```
Ожидание: все `PASSED`

- [ ] **Step 7: Commit**

```bash
git add src/common/bot/cbdata.py src/common/bot/keyboards/keyboards.py src/apps/device/controllers/bot/router.py src/common/bot/router.py
git commit -m "fix: replace unsafe split() parsing with typed CallbackData classes"
```

---

## Финальная проверка

- [ ] **Проверить ruff**

```bash
uv run ruff check --fix && uv run ruff format
```
Ожидание: нет ошибок

- [ ] **Запустить все тесты**

```bash
uv run pytest tests/unit/ -v
```
Ожидание: все `PASSED`

- [ ] **Проверить запуск бота**

```bash
uv run python -c "
import asyncio
from main_bot import main
print('Import OK')
"
```
Ожидание: `Import OK`
